# 8D 诊断 Agent · 端到端模拟运行记录

> **性质**：`sigma-agent-engineering` skill 的用法实例（模拟）。数据/产出均为**模拟值**，用于演示流水线机制、门禁与挂起点——不是真实生产数据。
> **生成**：2024-06-17 模拟 run `run_20240617_017`。驾驭层 = sigma-agent-engineering 编排，状态机 = DAG-8D。

---

## 触发工单
**#QA-2406-017**：A 产线 转子组件 RA-77 外径(OD)尺寸超差，良率连续三周 98.2% → 94.1% → 91.4%。质量经理点「启动 8D 诊断」。
- 关键特性：OD Ø50.000 ±0.050 mm（USL 50.050 / LSL 49.950）
- 工序：OP-30 外圆精车

---

## 状态字典（初始全 false）
```
{ MES数据已取, CpK已算, FMEA已检索, 历史8D已调, 交叉比对已做,
  根因已定, 报告已起草, 独立审查已过, 人工门已签 } = all false
```

---

## 运行记录

### D3 取真实数据（MCP 只读，模型不编数）
```
→ mes.query(line=A, part=RA-77, op=OP-30, window=3w, fields=[OD,lot,shift,ts])
← 1,842 条；最近批 B-2406-017 (n=125)：OD min 49.971 / max 50.061 / mean 50.018 / 超差 11 件(全偏大)
  ⚠ shift / 来料lot 有 14 条为空
```
**沙盒门禁 fire**（算 CpK 前 NaN 自愈）：
```
[sandbox] ValueError: NaN → df.dropna() 剔除 14 条 → 重跑 ✅（缺失记录另存，留给慢外环）
```

### 算过程能力（算力归代码）
```
USL,LSL=50.050,49.950 ; mean=50.018 ; sigma=0.0130 (n=111)
Cp  = (USL-LSL)/6σ = 1.28
Cpk = (USL-mean)/3σ = 0.82   ← CRITICAL (<1.33)
控制图：X̄ 近 21 点中 9 点连续上移 → 判异 fire
```
**模型解读（只解释不造数）**：`Cp 1.28 但 Cpk 0.82` = 分散尚可、**均值显著上漂**（OD 整体偏大贴 USL）→ 居中性/系统偏移问题。

### D4 检索 FMEA + 历史 8D（规则检索，按工序号精确命中）
```
fmea.lookup(op=OP-30外圆精车, mode=外径偏大)：
  ① 刀具后刀面磨损 RPN210  ② 来料硬度波动→切削让刀 RPN168
  ③ 主轴热伸长 RPN144      ④ 夹具重复定位漂移 RPN96
history.search_8d：命中 2 例（信用分三维剔除 1 例过期/已被 ECN 覆盖）
```

### 🔴 独立审查门禁 fire（生成者 ≠ 审查者）—— 挡下静默逃逸
- **生成者初版**：贴最高 RPN → 「根因 = 刀具磨损，建议换刀」，置信度 0.86。
- **审查者（读独立数据源）驳回**：
  - 刀具当前 520 件 / 寿命 800 件 → **寿命内**；
  - 若为磨损应随件数单调恶化，实测 OD 上漂与「件数」弱相关、与「来料批次切换日 05-27」**强相关**；
  - 判定 **REJECT**，打回（系迎合最高 RPN 的静默逃逸）。

### D4 根因（第二版 · 交叉比对三源 + 诚实置信度）
```
设备保养：主轴/刀具按表正常、刀具寿命内 → 排除磨损为主因
来料批次：良率拐点(05-27) 与 供应商B首批 LOT-2405-B/C/D 完全重合
          来料检验硬度：A批 HRC 28–30 → B批 HRC 32–34（偏高）
环境    ：温/湿度在控 → 排除
```
**根因假设（B）**：供应商切换后来料硬度偏高 → 切削力增大、刀具弹性让刀 → OD 均值系统性上漂 → Cpk 0.82。
**证据链**：①批次切换日=良率拐点 ②来料硬度记录 ③排除刀具磨损(寿命内+件数弱相关，审查独立确认)。
**置信度：中** ⚠ 尚缺 OP-30 实测切削力/后刀面直接证据，已标「待车间复现验证」，不冒充确凿。

---

## 8D 报告成稿（过 Schema 校验，8 个 D 缺一即熔断）

| D | 内容（模拟） |
|---|---|
| D1 | 小组：质量(组长)/工艺/采购SQE/产线 |
| D2 | RA-77 OD 超差，良率 98.2%→91.4%，Cpk 0.82(CRITICAL)，均值上漂 |
| D3 | 围堵：LOT-2405-B/C/D 在制品+成品 待冻结复检（→ 挂人工） |
| D4 | 根因：来料硬度偏高致切削让刀、OD 上漂（证据链；置信中·待复现） |
| D5 | 永久纠正：① SQE 对供应商B 来料设 HRC≤31 进料红线 ② OP-30 按硬度档位补偿刀补 +0.015 |
| D6 | 验证：调刀补后试切 50 件，目标 mean→50.000±0.010、Cpk≥1.33 |
| D7 | 防再发：来料硬度纳入进料必检 + Agent 取数前置校验 |
| D8 | 结案与表彰（待 D6 验证） |

---

## 🟡 不可逆动作 → 可逆门挂起（按可逆性，不按置信度）
```
拟动作：
  (a) 冻结 LOT-2405-B/C/D 在制品+成品  [不可逆·高后果]
  (b) 向供应商B 发来料异常通知函        [对外发送·不可逆]
  (c) OP-30 刀补 +0.015                 [可逆]
驾驭层裁决：
  (c) 放行（工艺自主）
  (a)(b) ⛔ 永远挂起等质量经理签 —— 即便 Agent 报 0.99 照样挂；人工门与置信度彻底解耦
[人工] 经理批准(a)；(b) 改为"先内部复检确认再发函" → 此 diff 写入判例库成种子
```
全 8 个 D 齐 ✅ → 交付 `8D_RA-77_20240617.pdf`（合规·可下发·已签）。

---

## 三周后 · 慢外环咬合（DMAIC C→D 飞轮）
看板报警：**夜班**根因返工率系统性高于日班（跳出控制带，非噪声）→ DMAIC 一圈 → 真因不在模型、在**第③步取数：夜班来料批次字段经常为空**（正是本次 dropna 掉的那 14 条）→ Improve 加约 50 行「来料批次完整性」前置校验 → **Control 固化成永久门禁 + 进下一版 CTQ 红线**。同一类失败再也过不来。

---

## 本次跑出来的 4 个「真在干活」点
1. 算 CpK 走沙盒、NaN 自愈，模型不碰数值；
2. **独立审查把「刀具磨损」这个迎合 RPN 的静默逃逸当场打回**（最值钱）；
3. 根因证据不足时诚实标「中·待复现」，不硬报确凿；
4. 冻结/发函按可逆性挂人工、与 0.99 置信度解耦；外加慢外环把当场被掩盖的来料缺失三周后固化成永久门禁。
